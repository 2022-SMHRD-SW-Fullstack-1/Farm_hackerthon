<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".board.BoardWriteActivity">

    <EditText
        android:id="@+id/etEditTitle"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:layout_marginTop="8dp"
        android:background="@android:color/transparent"
        android:ems="10"
        android:hint="제목을 입력하세요"
        android:inputType="textPersonName"
        app:layout_constraintEnd_toEndOf="@+id/tvMainTitle"
        app:layout_constraintStart_toStartOf="@+id/tvMainTitle"
        app:layout_constraintTop_toBottomOf="@+id/tvMainTitle" />

    <EditText
        android:id="@+id/etEditContent"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:layout_marginTop="16dp"
        android:background="@android:color/transparent"
        android:ems="10"
        android:hint="내용을 입력하세요"
        android:inputType="textPersonName"
        app:layout_constraintEnd_toEndOf="@+id/etEditTitle"
        app:layout_constraintStart_toStartOf="@+id/etEditTitle"
        app:layout_constraintTop_toBottomOf="@+id/etEditTitle" />

    <ImageView
        android:id="@+id/imgEditLoad"
        android:layout_width="170dp"
        android:layout_height="170dp"
        android:layout_marginTop="100dp"
        app:layout_constraintEnd_toEndOf="@+id/etEditContent"
        app:layout_constraintStart_toStartOf="@+id/etEditContent"
        app:layout_constraintTop_toBottomOf="@+id/etEditContent"
        app:srcCompat="@drawable/addimg" />

    <ImageView
        android:id="@+id/imgEditWrite"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_marginEnd="32dp"
        android:layout_marginBottom="32dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:srcCompat="@drawable/pencil" />

    <TextView
        android:id="@+id/tvMainTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:fontFamily="@font/playfairdisplaybold"
        android:gravity="center"
        android:text="수정하기"
        android:textSize="24sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>